version: "3.9"

services:
  core-tailor:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file:
      - ../backend/.env
    ports:
      - "8000:8000"
    volumes:
      - ../backend/output:/app/backend/output
      - ../backend/examples:/app/backend/examples
